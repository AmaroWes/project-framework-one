@model Aluroni.Models.Invoce

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Invoce</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ClientId" class="control-label">Client</label>
                <select asp-for="ClientId" class ="form-control" asp-items="ViewBag.ClientId"></select>
            </div>
            <div class="form-group">
                <label asp-for="DeliverymanId" class="control-label">Deliveryman</label>
                <select asp-for="DeliverymanId" class="form-control" asp-items="ViewBag.DeliverymanId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Bill" class="control-label">Bill</label>
                <input asp-for="Bill" class="form-control" />
                <span asp-validation-for="Bill" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
